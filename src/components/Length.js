import React, { useState, useEffect } from 'react';

function Length() {
  useEffect(() => {
    document.title = 'Length';
  });

  return (
    <div>
      Length
    </div>
  );
}

export default Length;
